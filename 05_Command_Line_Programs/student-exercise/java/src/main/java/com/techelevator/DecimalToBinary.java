package com.techelevator;

import java.util.Scanner;

public class DecimalToBinary {

	public static void main(String[] args) 
	{
		/*Scanner scanner = new Scanner(System.in);
		
		System.out.println("Please enter in a series of decimal values (seperated by spaces): ");
		String decimalValues = scanner.nextLine(); 
		int convertedDecimals = Integer.parseInt(decimalValues);
		
		String[] decimal = decimalValues.split(" ");
		int decimalValue = Integer.parseInt(decimalValues);
		
		for(int i = 0; i< decimal.length; i++) 
		{
			Integer.toBinaryString(decimal[i]);
		}
		
*/
	}
	 
	
}
